# 検証記録

## Cloudflare Workers公開確認（2026-09-12）

- 公開URL: <https://timepario.shiroemons.workers.dev>
- デプロイ対象: `main` の `d179f70`。Worker名: `timepario`。
- Version ID: `275923ca-f78d-45d7-a7e0-0ae4217cfe55`
- `pnpm build` / `pnpm size`: 成功。公開したJS・CSS・Service Worker・OGP画像は本番ビルドとバイト単位で一致。
- HTTPSのルート・3時計URL・manifest・Service Worker・OGP画像・オフライン画面: 200。存在しないアセット: 404。不正なタイムゾーン: 400。
- canonical・OGPのURLが公開先originを参照することを確認。実SNSのクローラーによる表示確認は未実施。
- Chromiumで3時計の表示、コンソールエラー0件を確認。Service Workerが有効化され、通信をオフラインにして再読み込みしても3時計を表示。オンライン復帰後の再読み込みも成功。
- PWAキャッシュ: `timepario-pwa-8e3b429c3db1e1ff1ac8`。確認画像: `output/playwright/public-offline.png`（Git対象外）。

## 環境

- 日付: 2026-09-12
- OS: macOS
- Node.js: 24.21.0
- pnpm: 11.19.0
- 以下の検証の実行対象: ローカル Workers runtime（Wrangler）。公開先の確認は上記に記録。

## 確認対象

| 分類 | 確認内容 | 結果 |
| --- | --- | --- |
| 静的解析 | strict 型チェック・Biome lint | 成功 |
| URL | 大文字・IANA・多階層・重複・順序・不正入力・二重デコード防止 | 成功 |
| 日時 | PST / PT・DST開始終了・分単位時差・深夜・月末・年末・うるう日・針角度 | 成功 |
| 自動判定 | ブラウザー設定優先・判定失敗の明示・明示URLの優先 | 成功 |
| 操作 | 検索・追加・変更・削除・並べ替え・共有・履歴 | 成功 |
| 復帰 | 非表示停止・現在時刻での再開・タイマー重複防止 | 成功 |
| 配信 | HTML / assets・404・CSP・エスケープ・定期通信ゼロ | 成功 |
| 表示 | 360×640・390×844・768×1024・1280×720・文字拡大 | 成功、画像確認済み |
| 言語 | 日本語・英語・未対応言語、SSR、557識別子の日本語名、検索・通知 | 成功 |
| 候補一覧 | 括弧付き略称、UTC時差の右揃え、DST・分単位時差・毎分更新 | 成功 |
| 文字盤 | 各地域の深夜・朝・昼・夕方・夜、境界での切り替え、色と文字のコントラスト | 成功 |
| 並べ替え | マウス・タッチ・ペン、確定・キャンセル、矢印、履歴復元 | 成功 |
| 演出 | ポインター追従・円状移動・移動先強調・離した位置からの移動、動きを減らす設定、連続操作 | 成功 |
| 比較基準 | 選択・解除、正負・分単位の差、DST、基準削除・非対応、並べ替え | 成功 |
| 全画面 | ネイティブ切替・終了・無操作・キーボード・フォールバック・1〜3時計 | 成功 |
| 拡大表示 | 全画面APIを呼ばない独立操作・起点への復帰・日英のスマホヘッダー | 成功 |
| PWA | manifest・アイコン・インストール要件・実通信遮断・キャッシュ更新・失敗通知 | 成功 |
| OGP | 日英の地域名・絶対URL・canonical・Twitter Card・共有画像・noindex | 成功 |
| 本番ビルド | esbuild / Wrangler dry run・gzipサイズ | 成功、予算内 |

## ブラウザー

コピーUX調整後の最終実行は全333件（各111件）成功、54.2秒。単体160件、型チェック、lint、本番ビルドも成功。リンクをコピーする意味が伝わるラベルとアイコン、2秒間の成功表示、連打防止、手動コピーへのフォールバックを検証した。ヘッダーは淡い緑・控えめな枠線へ調整し、日英・360/1280px・100/200%文字で、コピー前後のボタンの位置と寸法が1px以内で維持されることを確認。最新画像は327枚。最終アセットとHTTPレスポンスも一致した。

最新の追加依頼（拡大表示・PWA・スマホ上下矢印・OGP）では、通常機能315件と実Service Workerを含むPWA9件の分割実行で、重複を除いた全321件（各107件）成功。通常機能は51.9秒、PWAは5.0秒。型チェック・Biome・単体160件・本番ビルドも成功した。OGP追加後の最終ビルドでPWAを再検証し、HTTPから画像・メタ情報・本番ファイルとの一致を確認した。

PWAではChromiumのブラウザーオフライン設定に加え、隔離したプロキシで接続先への通信を実際に遮断した。Firefox / WebKitはPlaywrightのオフライン設定に差があったため、プロキシの通信遮断で実Service Workerのフォールバックを検証。未訪問URL・再読み込み・不正URL・並べ替え・オンライン復帰が成功した。ChromiumのCDPによるmanifest解析・インストール要件エラーは0件。OSへの実インストールはしていない。

PWA更新失敗時に旧キャッシュを残すこと、SHA-256不一致・初回準備失敗の検出、TimePario以外のキャッシュを削除しないこと、3秒の通信タイムアウトを単体テストで確認。初回準備が終わった後の時計表示は引き続き定期通信を行わない。テスト用開発サーバーの終了時に子プロセスが残る問題は、PlaywrightのgracefulShutdownでSIGTERMを送り、開発スクリプトの終了処理を通す設定へ変更した。

スマホの並べ替え矢印は実SVGの座標から上下方向、PCでは左右方向であることを確認。画面幅変更と動きを抑える設定でも一致した。OGP画像は1200×630で生成・目視し、配信画像と成果物のバイト一致を確認。実SNSのクローラー検証は公開URLが必要なため未実施。

Playwright 1.63.0で285件（各95件）を検証。Chromium 153.0.8010.12 / Firefox 155.0 / WebKit 26.6を実行。本番ビルドJS/CSSとローカル配信レスポンスが一致することも別途確認した。Chromiumのタッチ操作はCDP入力、Firefox / WebKitのタッチ・ペンは合成Pointer Eventsで検証。Playwright のエンジン検証はすべての実機・OS・ブラウザーバージョンを保証するものではない。

E2Eでは日時を固定し、通常時計は2026-07-01T12:34:56Z、時間帯のテストは現地の00:00・05:00・12:00・16:00・19:00境界などを使用。単体テストは2024年のDST・月末・年末・うるう日等を明示的な期待値で検証した。単体テストは合計147件成功。

正常な時計画面でコンソールエラー0件。PWA初回準備の完了後、65秒分の仮想時間進行中に追加リクエスト0件。CSPのconnect-srcはPWA用の同一オリジン通信だけを許可し、コードに時刻取得API・WebSocket・定期通信を含めない。JavaScript無効時の未確定表示と説明は3ブラウザーで確認した。

## スクリーンショット

最新の画像は255枚。拡大表示ボタンを追加した日英のスマホヘッダーと、上下に変わる矢印の画像を追加して確認した。以下は全画面対応までの237枚の記録。

`output/playwright/` に237枚保存（各ブラウザー79枚）。通常画面の4画面サイズ、PC1/2時計、200%文字拡大、長いIANA名、検索ダイアログ、日本語・英語の時間帯表示に加え、全画面の360×640・1280×720・1920×1080、1〜3時計、日英、100%・200%文字サイズを取得した。Chromiumの全サイズと拡大を目視し、Firefox / WebKitの代表画像も確認した。追加分は日本語PCの3色文字盤とPST/PDT候補、深夜の日本語PC・スマートフォンと英語200%拡大、WebKitの日本語スマートフォン、Firefoxの分単位時差候補、Chromiumの日本語200%拡大を目視した。768px幅でも3時計の時刻と日付が同じ高さに並ぶ。360×640の通常3時計は全カードが画面内に収まり、補足とフッターは必要に応じスクロールする。文字拡大時は時計の数字を縮めず縦方向に配置する。

全画面の追加分はChromiumの1280pxで1〜3時計、1920pxの1時計、小画面の縦配置、Firefoxの英語200%表示、WebKitの日本語3時計を目視した。レイアウト画像はFullscreen API非対応を明示的に模擬した拡大表示で取得。別の操作テストでは3ブラウザーとも実際にネイティブ全画面へ入り、終了後に時刻・URL・比較基準を維持した。拒否・非対応・申請中Escape・ブラウザー側解除は明示的なモックで検証した。

初回検証で、全画面中に非表示になる開始ボタンをロールで検索するテストの問題と、WebKitのTabキー操作で終了ボタンへ到達しない問題を検出。テストはIDによる参照へ変更し、アプリ側では全画面中のTab/Shift+Tabを終了ボタンへ誘導するよう修正した。全画面操作21件は再実行で成功。最終全体実行は282件成功、スクロール後の表示比率がサブピクセル丸めで0.99593となるレイアウト検証3件が失敗したため、実際の上下左右座標が画面境界±1pxに収まる判定へ修正した。

修正後の全画面レイアウト54件を再実行し、すべて成功（12.9秒）。全体実行と修正対象の再実行を合わせて、285件すべて成功を確認した。小画面と文字200%でも最後の時計の時刻・比較時差へスクロールで到達でき、画面内へ収まる。

## サイズ・セキュリティ・開発環境

直近の追加分では、基準選択時の日本語PC・スマートフォン、夕方の文字盤、ドラッグ中のPCと確定アニメーション途中のスマートフォンを目視確認した。外周から数字を8 SVG単位内側へ寄せ、目盛りとの間隔を確保した。基準との差は既存のカード下部に表示し、3時計の高さを増やしていない。

ポインター追従の修正後は、PCとスマートフォンの移動先に重なるカード、および2時計で領域外へ移動したカードの画像を確認した。テストでは円状の移動に対するカード座標の追従、ドロップ直前と直後の位置・寸法の連続性、横スクロールの防止、元の位置や範囲外でのキャンセルを検証した。

`pnpm build`後、`pnpm size`（Node標準gzip）と実際のローカルHTTPレスポンスで実測:

| リソース | bytes | gzip bytes |
| --- | ---: | ---: |
| `/assets/app.js` | 55,042 | 20,387 |
| `/assets/app.css` | 16,852 | 4,546 |
| `/sw.js` | 2,902 | 1,477 |
| `/manifest.webmanifest` | 567 | 281 |
| `/icons/apple-touch-icon.png` | 1,970 | 1,886 |
| `/icons/icon-192.png` | 2,187 | 2,064 |
| `/icons/icon-512.png` | 6,628 | 5,747 |
| `/icons/maskable-512.png` | 5,904 | 4,842 |
| `/favicon.svg` | 313 | 224 |
| `/` HTML（日本語） | 6,943 | 2,314 |
| `/jst,pt,utc` HTML（日本語） | 10,574 | 2,673 |
| `/offline` HTML（日本語） | 5,824 | 2,142 |
| `/og.png`（共有クローラー用） | 54,009 | 51,655 |

JS+CSS+favicon+日本語HTMLのgzip合計はトップ27,471 bytes、3時計27,830 bytes。全PWAファイルを1回ずつ足したgzip合計は43,596 bytes（通常ページHTML・OGP画像・HTTPヘッダー・TLS除外）。初回キャッシュ作成には再取得があり、実際の総通信量を表す値ではない。Workerビルドは160.21 KiB / gzip39.83 KiBであり、ブラウザー配信サイズには含めない。日本語をUTF-8で直接出力し、メインJS20KiB・CSS10KiBの目標内を維持。SWは別途1,477 bytes gzip。アセットは `public, max-age=0, must-revalidate`、SWは `no-cache`、HTMLは `no-store` をHTTPで確認。初期実装時の全212依存のauditは既知の脆弱性0件で、その後パッケージ変更はない。

ローカルChromiumで候補423件の初回表示処理66.4ms、PST検索処理12.6msを計測した。開発端末での1回の同期処理時間であり、実機スマートフォンの応答時間を保証する測定ではない。

開発時のソース変更でブラウザーバンドルが再生成されることを実際の修正・再読み込みで確認。初回ビルド完了後にWranglerが起動する。検証の初回にはmacOSサンドボックスのブラウザー起動制限があったが、承認されたローカル実行で解消。複数Wranglerの同時起動によるSQLite競合は検証サーバーを1つにして解消した。

ドラッグ追従の検証中、起動済みサーバーが監視する出力ディレクトリを本番ビルドで削除・再作成した後、JS/CSS/faviconが500を返した。サーバーを再起動して復旧し、ビルドの清掃を「ディレクトリは維持し、古いファイルだけ削除」に修正。監視対象のinode維持、不要なソースマップの削除、再起動なしでのJS/CSSの200応答と本番ファイルとの一致を確認した。隔離したテスト用ディレクトリでは、シンボリックリンクを追跡せず削除することも確認した。

## 未確認事項

実機スマートフォン、スクリーンリーダーによる音声確認は未実施。
